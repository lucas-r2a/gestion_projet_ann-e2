{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1 class="mb-4 fw-bold">Team</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ team.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_team_index') }}" class="btn btn-secondary mt-3">⟵ Return</a>

    <a href="{{ path('app_team_edit', {'id': team.id}) }}" class="btn btn-secondary mt-3">edit</a>

    {{ include('team/_delete_form.html.twig') }}
{% endblock %}
