{% extends 'base.html.twig' %}
{% block title %}Assignations{% endblock %}

{% block body %}
<div class="bg-white p-6 rounded-xl shadow">

    <h2 class="text-2xl font-semibold mb-4">Mes Tâches</h2>

    {# <a href="{{ path('app_user_add_assignation', {'id': user.id}) }}"
       class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded mb-4 inline-block">
        + Assigner une tâche
    </a> #}

    <table class="w-full bg-white border-collapse shadow rounded-md overflow-hidden">
        <thead class="bg-gray-200">
            <tr>
                <td class="px-4 py-3">Titre</td>
                <td class="px-4 py-3">Statut</td>
                <td class="px-4 py-3">Actions</td>
            </tr>
        </thead>

        <tbody>
        {% for a in assignations %}
            <tr class="border-b">
                <td class="px-4 py-3">{{ a.tache.titre }}</td>
                <td class="px-4 py-3">{{ a.tache.statut }}</td>
                <td class="px-4 py-2 flex gap-2">
                    <a href="{{ path('app_tache_edit', {'tache': a.getTache().id}) }}"
                    class="text-yellow-600 hover:underline">
                    Modifier
                    </a>
                    

                    {# <form method="post"
                          action="{{ path('app_user_remove_assignation', {'id': a.id}) }}"
                          onsubmit="return confirm('Retirer cette assignation ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('remove_assignation' ~ a.id) }}">
                        <button class="bg-red-600 px-3 py-1 text-white rounded">X</button>
                    </form> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3" class="text-center py-4 text-gray-500">Aucune assignation</td>
            </tr>
        {% endfor %}
        {% for a in tachesEquipe %}
            <tr class="border-b">
                <td class="px-4 py-3">{{ a.titre }}</td>
                <td class="px-4 py-3">{{ a.statut }}</td>
                <td class="px-4 py-2 flex gap-2">
                    <a href="{{ path('app_tache_edit', {'tache': a.id}) }}"
                    class="text-yellow-600 hover:underline">
                    Modifier
                    </a>
                    

                    {# <form method="post"
                          action="{{ path('app_user_remove_assignation', {'id': a.id}) }}"
                          onsubmit="return confirm('Retirer cette assignation ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('remove_assignation' ~ a.id) }}">
                        <button class="bg-red-600 px-3 py-1 text-white rounded">X</button>
                    </form> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3" class="text-center py-4 text-gray-500">Aucune assignation</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>
{% endblock %}
